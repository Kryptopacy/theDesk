{
  "version": 1,
  "id": "mdt_72hourfund_v1",
  "created_from": "Fund policy v1 (pending final plain-English confirmation from founder at approval time)",
  "created_at": "2026-09-05T00:00:00Z",
  "approved_at": null,

  "symbols": ["BTCUSDT"],
  "quote_asset": "USDT",
  "budget_usdt": 10,

  "risk": {
    "max_order_notional_usdt": 5,
    "daily_loss_cap_usdt": 2,
    "max_open_orders": 1,
    "forbidden_actions": ["margin", "futures", "transfer_out"]
  },

  "schedule": { "recurrence": "daily", "time_utc": "12:00" },

  "rules": [
    { "id": "r1", "kind": "recurring_buy", "quote_amount_usdt": 5, "trigger": { "type": "schedule" } },
    { "id": "r2", "kind": "take_profit_full", "trigger": { "type": "price_gain_since_avg_entry", "pct": 10 } }
  ],
  "_note": "r2 is a full exit because a partial sell of a ~5 USDT position falls below Binance's ~5 USDT min notional — a partial TP rule is only compilable for larger budgets.",

  "kill_conditions": [
    { "metric": "daily_pnl_usdt", "op": "<=", "value": -2 }
  ]
}
